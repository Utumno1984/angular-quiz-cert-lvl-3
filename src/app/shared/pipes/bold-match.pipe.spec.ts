import { BoldMatchPipe } from './bold-match.pipe';

describe('BoldMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
